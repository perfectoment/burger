<h1>Burgers</h1>

<h2>Burgers that are not eaten</h2>

<ul>
  {{#each cats}}
    {{#unless sleepy}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have benn devoured</h2>

<ul>
  {{#each cats}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Type</label>
    <input type="text" id="br" name="name">
  </div>

  <div class="form-group">
    <label for="eat">Eat the Burger?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="undevoured" value="0"> Not Eaten
  </div>

  <button type="submit">Add Burger</button>
</form>